material_head_sha=25aa48a0246867bb9d767c8b9ae658bd1b0afbcc
base_sha=c623341f729099f72c96c3908e52d0098f0a3ebd
pr=628

apps/api/src/repositories/dashboard.ts
- account opening balances grouped by upper(currency)
- cash movements grouped by upper(movement.currency)
- realized month totals grouped by upper(movement.currency), kind, status
- planned totals grouped by upper(movement.currency), kind, status
- currencyBlocks sorted by currency code
- scalar compatibility emitted only when currencyBlocks.length === 1
- recentItems include normalized currency

apps/api/src/financial-summary-multi-currency.integration.test.ts
- BRL delta expected: available=10800 income=1100 expenses=300 planned=500
- USD delta expected: available=21800 income=2200 expenses=400 planned=700
- mixed response asserts scalar compatibility fields absent
- recent issue items assert both BRL and USD currencies

packages/domain/src/budgets.ts
- summarizeBudgetUsage passes budget.currency to sumActualAmount
- sumActualAmount requires transaction.categoryId and transaction.currency to match budget
- unbudgeted dashboard key combines currency and categoryId
- dashboard summaries sorted currency then category

apps/api/src/repositories/budgets.ts
- persisted usage query filters organizationId, financialProfileId, categoryId, occurredOn period and upper(currency)=upper($6)

packages/ai/src/insights.ts
- explicit currency is strict ISO 4217 input
- absent currency is inferred only when scoped native data has exactly one currency
- zero currencies or multiple currencies without explicit filter throws

packages/ai/src/insights-currency-contract.test.ts
- single USD source expects USD, never BRL
- BRL+USD without explicit currency expects error
- explicit USD mixed source expects 5500 and excludes BRL source
- invalid explicit currency expects ISO 4217 error

apps/web/src/dev-server/dashboard-page.ts
- canonical consumer input is currencyBlocks
- each block is rendered independently
- each metric formatter receives block.currency
- recent item formatter receives item.currency

apps/web/src/dev-server/dashboard-page-currency.test.ts
- asserts BRL section occurs before USD section
- asserts exactly two currency-summary sections
- asserts recent item metadata includes USD

scope/filter preservation
- dashboard aggregate SQL retains organizationId + financialProfileId predicates in every aggregate query and current-month/current-date predicates.
- budget persisted usage retains organizationId + financialProfileId + categoryId + occurredOn period predicates and adds currency predicate.
- reports and financial-assistant aggregate implementations were inventoried as already currency-safe and are not modified by PR 628, preserving their existing account/card/period filters.
- PR 628 changed-file inventory contains no workflow files and no destructive file deletions.
