name: Issue 619 Handoff Staging

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Issue 619 handoff package is being staged."
