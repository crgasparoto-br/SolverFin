{
  "name": "@solverfin/ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Abstracoes de IA, schemas estruturados e politicas de uso seguro.",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "test": "rm -rf ../../.tmp-tests/ai && tsc -p tsconfig.test.json && node ../../.tmp-tests/ai/index.test.js",
    "build": "tsc --noEmit"
  }
}
