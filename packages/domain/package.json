{
  "name": "@solverfin/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Regras e entidades do dominio financeiro, isoladas de infraestrutura.",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "test": "rm -rf ../../.tmp-tests/domain && tsc -p tsconfig.test.json && node ../../.tmp-tests/domain/audit.test.js && node ../../.tmp-tests/domain/tenant.test.js && node ../../.tmp-tests/domain/tenant-authorization.test.js",
    "build": "tsc --noEmit"
  }
}
