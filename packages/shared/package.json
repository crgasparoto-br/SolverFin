{
  "name": "@solverfin/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tipos, utilitarios e contratos compartilhados entre apps e pacotes.",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "test": "rm -rf ../../.tmp-tests/shared && tsc -p tsconfig.test.json && node ../../.tmp-tests/shared/masking.test.js",
    "build": "tsc --noEmit"
  }
}
